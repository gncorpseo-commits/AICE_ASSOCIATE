# 아침 먹고 오면 이것만 하세요 (Windows)

PowerShell을 열고 세 줄만 입력합니다.

```powershell
cd C:\project\GN_Build_Up\AICE_Associate_samples
git pull
.\start_study.bat
```

첫 실행은 Python/라이브러리 설치 때문에 **5~15분** 걸릴 수 있습니다. 창을 닫지 마세요.

끝나면 브라우저가 열립니다.

1. `regression` 폴더 → `problem.ipynb`  (내비게이션 회귀, 먼저 풀기)
2. 다 풀면 `classification` → `problem.ipynb`  (와인 품질 분류)
3. 채점은 같은 폴더의 `solution.ipynb` / `answer_key.md`

노트북과 CSV는 이미 같은 폴더에 있습니다. 셀은 위에서부터 Shift+Enter 로 실행하세요.
